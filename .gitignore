# 构建中间产物（本地生成，不入库）
Alltear_UnrealMemoryToolsNewMcp/build/
Alltear_UnrealMemoryToolsNewMcp/build-termux/

# 编译产物（adb push 用，不入库）
Alltear_UnrealMemoryToolsNewMcp/outputs/

# 调试残留
*.bak

# Python 虚拟环境与缓存（本地生成，不入库）
.venv/
venv/
__pycache__/
*.pyc

# Python 打包产物
dist/
*.spec
*.egg-info/
*.egg

# 构建产物
build/

# 临时文件
*.zip

# 本地 dump 校验产物（数十 MB，不入库）
_dump_verify/

# clangd 用：指向 build/ 下 CMake 生成的编译数据库（build/ 本身已 ignore）
Alltear_UnrealMemoryToolsNewMcp/compile_commands.json
